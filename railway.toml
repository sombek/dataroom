[build]
builder = "nixpacks"
buildCommand = "python311 -m pip install -r requirements.txt"


[deploy]
startCommand = "python311 api/app/main.py"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "never"

